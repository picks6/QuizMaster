import React, {useState} from 'react';
import { Card, Button } from 'semantic-ui-react';
// import CardFlip from '../quizmaster/CardFlip';

const Deck = ({ deck }) => { 
  // Deck ID must be passed

  return (
    <div>
      {/* {cards.map((el) => <CardFlip sideA={el.sideA} sideB={el.sideB} />)} */}
    </div>
  )
}

export default Deck;